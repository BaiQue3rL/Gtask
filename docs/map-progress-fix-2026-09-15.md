# 原神地图人工验收问题修复

2026-09-15，软件维护；未修改地图公共目录或线上版本。

## 原因与修复

替换前备份与安装后数据库中的沉玉谷始终是同一稳定条目，位于璃月二级目录。18:13 的启动同步把进度从 100% 改为 0%；未完成筛选隐藏璃月后，旧显示逻辑使它看起来像新增一级目录。

本次通过现有官方客户端只读提取地图字段，核实沉玉谷 `id=10/type=Offering` 的零占位与三个子区 `11/12/13` 的完整进度并存。上轮把所有零值一律视为直接进度，移除了历史特殊处理，造成回归。

- 为这一已核实接口形态设置有限例外：预期三个子区完整、唯一且全满时确认 100%；明确有未完成子区时记录未完成，不编造汇总百分比；完全缺少有效完成证据时跳过汇总写入。
- 普通地区真实零值和独立总进度继续优先。至冬 52% 与五个已开放子区 100% 并存符合用户确认的含义；不以子区平均数覆盖总进度。
- 保留未完成筛选。父项被隐藏时，子区显示“所属地区 /”上下文；公共目录、稳定 ID 与父子关系均不改变。
- 汇总项明确未完成但无百分比时清除过期的 100%，避免被显示层再次当成完成。手动完成保护同时保留进度值。

## 验证与交付

- 地图、解析、快照及审计专项 57 项通过；完整测试 445 项通过、12 项跳过；类型、生产构建、清单校验与差异空白检查通过。
- `scripts/audit-map-regression.mjs` 使用独立路径的真实打包程序验证筛选、恢复展开、父地区上下文和 540/750 像素窗口；零页面错误。截图和结果位于 `C:\Users\Administrator\AppData\Local\Temp\gtask-ui-audit-Z2Gl7B`。
- 18:26 正常关闭旧进程、备份并替换 `D:\Git\Gtask`，校验全部 35 个文件后重新启动。
- 18:26:45 实际自动同步确认：沉玉谷已完成、100%，仍属于璃月二级；至冬 52%，五个二级目录仍在且全为 100%。启动日志无错误，自定义数据与加密凭据保留。
- 备份：`D:\Git\Gtask-backups\20260915-182600-map-fix`；其中包含替换前程序、SQLite 在线备份和 userData。
- 安装后 app.asar SHA-256：`6789735fafc60cf40d2a030969e91f674ed263cb0581d1b65173150133a75488`。
- 软件版本仍为 1.1.1，未提交、推送或发布。本记录补充前一轮审计报告，不能将旧测试通过等同于没有遗漏。
