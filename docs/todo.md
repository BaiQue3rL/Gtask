# 产品与开发待办

更新时间：2026-07-28（Asia/Shanghai）

本文件是后续开发的待办基准；已经确认的新规则优先于旧解析器和旧测试中的完成阈值。

## P0：个人同步底层架构重构（核心数据面已完成）

完整决策见 [sync-architecture-redesign.md](./sync-architecture-redesign.md)。当前“全部个人数据进入 Codex”只是临时现状，不再作为最终架构目标。

- [x] 新增规范项目、来源绑定、个人状态、字段语义和同步审计的清晰数据边界。
- [x] 保存“平台 + 接口 + 官方 ID → Gtask 项目 ID”持久绑定，停止用单一 `remoteKey` 混合内部身份与来源身份。
- [x] 从语义审核历史和稳定模式键安全回填可确认绑定，不猜测、不清空真实数据。
- [x] 恢复周期挑战和受支持地图探索的机械快路径；只有未绑定、冲突或未知字段进入 Codex。
- [x] 将活动完成状态改为“完成 / 未完成 / 无法判断时不写”的内部三态策略。
- [x] 保存带版本的字段语义配置，机械快路径只使用 `active` 配置。
- [x] 将周期与已绑定地图的语义审核缩小为异常队列；活动保留必要的状态语义审核。
- [ ] 增加 Codex 批量建立地图绑定及原始观察值显式重放，继续降低首次大地图同步耗时。
- [ ] 通过影子比对和四游戏回归确认新旧结果，再移除当前全量 Codex 个人审核路径。

## P0：重构前可靠性基线

- [ ] 保存当前未提交工作树并明确唯一源码、插件和试用包版本，避免修复源码却运行旧包。
- [ ] 将 SQLite 写入收敛为可重试单写入队列，彻底消除定时任务、同步和界面写入之间的 `database is locked`。
- [x] 完成同步取消全链路：公开任务、个人接口、语义候选和已领取 Worker 取消后均不得继续写入，同时不影响其他任务。
- [x] 允许被拒绝或旧协议处理过的语义候选重新入队；存在未解决失败时不得结算成功。
- [x] 增加桌面端、Codex 插件和 MCP 协议版本握手与不兼容提示。
- [ ] 增加官方接口结构版本检测，区分接口变化、凭据失效、风控验证和临时网络错误。
- [x] 使用稳定身份的单向哈希作为账号/角色/游戏隔离键，防止切换 UID 后进度串号且不暴露原始标识。
- [ ] 验证升级前备份、失败回滚和旧数据库兼容读取，并决定正式版是否继续使用 `node:sqlite`。
- [ ] 根据 [Codex 代理与 WebSocket 重试排查记录](./codex-proxy-websocket-troubleshooting.md) 完成进程级 HTTP/SSE 兼容模式：检测真实 WebSocket 五次超时后允许用户让本次 Gtask Worker 跳过 WebSocket，不静默修改全局 Codex 配置。
- [ ] 禁止通过永久用户级 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 或 `NO_PROXY` 修复 Codex；代理提示只提供继续等待、本次 HTTP 兼容模式和用户自行选择全局/TUN。

## P1：重构后的产品与发布加固

- [ ] 提供不包含凭据和账号标识的脱敏诊断包，便于定位接口变化、协议版本、任务状态和数据库争用。
- [ ] 为长期增量清单建立有证据的归档策略；单一来源遗漏不能触发删除。
- [ ] 地图和活动列表达到性能阈值后启用折叠懒加载或列表虚拟化。
- [ ] 完成单文件便携版临时解包、重复启动、杀毒软件、路径重定向、异常断电和恢复后重启验收。
- [ ] 完成安装包、代码签名、SmartScreen、自动更新、崩溃恢复和许可证发布检查。

## P0：完成状态与同步正确性

- [x] 四款游戏统一深渊/挑战完成规则：个人接口只要返回“本期存在有效挑战记录”就标记已完成；只有本期完全没有挑战记录才是未完成。不再要求满星、最高难度、全部奖励或最终层，因为每个用户对“打完”的预期不同。
- [x] 为官方接口定义结果唯一的“存在记录”机械条件并补测试：原神三种挑战、星铁四种挑战、绝区零两种挑战、鸣潮三种挑战均执行同一产品语义；不按标题或推测字段判断。
- [x] 公开资料只能建立挑战名称与周期，绝不能把“有排期”当成用户有挑战记录。
- [x] 周期事项和活动不显示百分比；只有地图探索显示百分比。
- [x] 2026-07-24 重新打包并真实复测原神周期：稳定设备 ID 与官方注册指纹成对发送后无需再次验证，深境螺旋、幻想真境剧诗、幽境危战三项均成功更新并按“存在记录”完成。
- [x] 2026-07-24 实测星铁四种周期模式与绝区零两种周期模式：星铁四项无需验证成功更新；绝区零两项成功更新，当前式舆无记录保持未完成、危局有记录保持完成。

## P0：任务版块版本周期

- [x] 主线任务、支线任务卡片增加当前游戏版本结束倒计时，但界面沿用其他版块的简洁文案，只显示“剩余 ×天/小时/分钟”，不得显示“版本结束倒计时”等额外前缀。
- [x] 为每款游戏保存 `versionKey`、版本开始/结束绝对时间和服务器时区；时间由 Codex 通过中文官方版本公告核验后，可在全局“同步清单”或任务版块“版更校时”时写入。
- [x] 主线与支线共享当前版本结束时间。版本结束后两项自动恢复为未完成并清除旧完成时间与手动完成锁。
- [x] 任务版块增加独立“版更校时”按钮，只校正当前版本键和开始/结束时间，用于官方临时调整版本排期；不得新增、删除主线/支线，不得直接改写两项完成状态。
- [x] 任务版块继续保持固定两项，不提供“删除已完成”和“新增”按钮。
- [x] 为任务版块保存独立同步状态与最后成功时间；只有版本资料成功核验并写入后才更新，失败时保留上一次成功的版本时间。
- [x] 到期但尚未取得下一版本时间时先完成本地重置，并显示“待同步新版本时间”，不得按固定天数猜测结束时间。

## P1：活动玩法标签

- [x] 活动卡片增加一个或多个轻量玩法标签，不新增版块。候选标签可包含签到、战斗、战棋、射击/FPS、跑酷、解谜、音游、经营、肉鸽、剧情等，并允许以后扩展。
- [x] Codex 根据中文官方活动说明、玩法描述和可验证来源给出标签；本地代码只做数量、长度和结构校验，不按活动标题关键词猜测。置信度与证据沿用整条公开资料记录的现有协议。
- [x] Codex 必须主动核验并补齐新旧活动玩法标签；确实无法确认时写入“未知”，禁止“待识别”。
- [x] 将补齐要求升级为程序硬约束：接单时逐项返回全部有效空标签/未知活动，少提交一项即拒绝整批；标签专用回写保留原时间、完成状态、来源和个人进度，核验后仍未知则标记部分完成并在下次继续重试。
- [x] 活动版块增加轻量标签筛选；同一活动允许多个标签。
- [x] 四款游戏活动版块统一增加“全部活动 / 限时活动 / 常驻活动”类型筛选，并与玩法标签筛选使用同一套界面样式。
- [x] SQLite v15 迁移兼容现有活动，旧活动没有标签时不影响显示、排序和同步；个人进度同步不覆盖公开资料已有标签。

## P1：同步状态与界面

- [x] 右上角状态文案明确为“全局清单 · 已同步/未同步”，避免被理解为最近任意一次个人进度同步。
- [x] 保持现有规则：右上角只记录成功的全局 `target=all` 清单同步；活动、周期、地图各自显示版块最后成功时间，版块同步不冒充全局成功。
- [x] 已移除顶部旧的“最近任意同步成功时间”，避免与全局时间同时显示两个日期；当前原神全局最后成功为 2026-07-21 23:08（北京时间）。
- [x] 完成全局/版块状态标识、时间戳、长标题和最小窗口的视觉验收；页面不再重复声明系统边框之外的最小宽度。
- [x] 全部版块统一倒计时预警：距离结束严格小于 72 小时时，将“剩余 ××”标为红色；等于或大于 72 小时保持普通样式。“距离开始 ××”不使用过期红色预警。
- [ ] 下一次普通同步覆盖测试期间遗留的脱敏诊断成功文案；正式源码已经移除诊断输出。

## P1：三游戏回归与地图

- [x] 2026-07-24 原神地图个人进度同步成功更新 74 项；沉玉谷父项、沉玉谷·上谷、沉玉谷·南陵与来歆山均保留真实 100%，公开父项与个人子项未再被 0% 覆盖。
- [x] 2026-07-25 联网复核四款游戏当前顶层区域；原神独立地图层同时与官方互动地图接口逐项核对。公开资料同步只增量新增，不删除旧区域、不覆盖个人进度。
- [x] 2026-07-24 完成三款米游社游戏活动语义候选实测：候选可脱敏入队、由 Codex 领取并逐条结束；已用有中文证据的原神活动验证高置信回写，用“反贪「砖」家”等其余 16 项验证证据不足时安全拒绝。幽境危战误入活动候选时也被识别并拒绝，未污染活动清单。
- [x] 跨游戏回归活动/周期分类，确保深渊挑战不进入活动版块、未开始活动不能完成、无时间的非活动条目不能混入活动。
- [x] 原神、星铁按版块真实复测：修正战绩请求漏发稳定设备 ID 后，原神周期、地图和活动原始状态以及星铁周期均无需再次验证成功返回；隔离官方战绩页方案暂不需要实施。

## P2：Codex 与同步体验

- [x] 将同步数据决策统一交给 Codex：该项已实现但现被认定为负担过重的临时架构；后续按 P0 重构为“Codex 建立语义与异常决策，软件复用已确认绑定和规则”。
- [x] 公开资料和个人语义回写使用 Codex 明确指定的事项 ID 进行严格合并，不再由本地标题/时间启发式规则二次决策。
- [x] Codex 可把四款游戏中确认错误、重复或失效的同步项移入回收站；手动事项及主线、支线、周常保持同步删除保护。
- [x] 语义核验队列及通过/拒绝原因保留在 SQLite 与 MCP 审计接口中，不向普通用户展示内部判定术语；桌面端只显示正在同步、真实重试、等待验证、完成或失败等可理解进度。
- [x] 原神、星铁、绝区零的活动个人状态统一进入 Codex 核验；本地只脱敏官方 ID、原始状态和可机械确认的 Unix 时间，不按标题关键词、状态枚举或无时区文本猜分类、时间和完成。
- [x] 鸣潮适配器沿用相同边界：探索百分比、Unix 时间、稳定 ID 和“存在挑战记录”由本地机械转换；活动与其他非确定性语义不在本地猜测。
- [x] 库街区客户端为临时服务错误、限流和网络中断增加最多三次的真实重试与进度回传；登录过期、风控和确定性业务错误立即停止，不进入无限验证或重试。
- [x] 点击同步后自动启动本机 Codex CLI；采用动态并发领取跨游戏、跨版块任务，启动、插件初始化、模型连接、真实 `1/5` 重试、检索、核验、整理、写入均实时显示。未接单任务持续排队，已接单任务 15 分钟没有心跳则重新排队；不再提供“打开 Codex 接单”按钮。
- [x] 首次连接改为应用内三步引导：正式包内置 Codex 插件与 MCP 配置模板，但不静默安装；用户在 Codex 安装页确认一次后返回应用重新检测，无需单独填写 MCP 命令。
- [x] Codex 模型连接出现真实 `1/5` 重试时显示非强制代理诊断：说明只开启系统代理可能导致 WebSocket 超时和同步明显变慢，建议全局/TUN 模式，并提供“复制排查提示词 / 继续同步”选择；个人数据重试不会误触发。
- [x] 全局公开资料同步按版块独立保存但实行完整覆盖门槛：已核验版块立即安全写入，缺失任务、活动、周期或地图时同一 Worker 继续使用 Codex 原生检索；四版块齐全才显示全局成功，确实穷尽检索后显示失败并保留已取得结果。单 Worker 失败不结束其他并行任务。
- [ ] 评估保留联网检索型 AI API 扩展位；只有能真实联网并返回逐项来源的提供方才可接入，普通聊天 API 不用于同步。

## P2：鸣潮、初始验收与发布

- [x] 完成 DPAPI 凭据底座、鸣潮个人数据机械层及库街区应用内登录：软件自动生成 DID，用户只完成官方滑块与短信验证码，Token 不显示也不经过剪贴板；读取角色并验证 BAT 成功后才加密落盘，已删除手动 Token + DID 导入入口。
- [x] 使用真实库街区应用内登录完成滑块、短信、角色读取、BAT 新格式兼容与 DPAPI 落盘验收。
- [x] 使用已保存的库街区凭据完成逆境深塔、冥歌海墟、终焉矩阵和地图探索端到端验收；已兼容官方 `data` JSON 字符串与新版 BAT 返回，统一三个挑战的稳定模式键，个人同步不再用异常服务器时间覆盖公开排期。
- [ ] 研究鸣潮 QQ 机器人常用的“仅 UID 公共查询”能力边界；若确有无需账号令牌且来源可靠的字段，可作为低权限补充，但不能冒充完整个人进度。
- [x] 现有数据回归通过后创建手动安全备份；从零初始状态、重复同步、时区、失败保留和手动完成锁使用隔离临时数据库完成自动验收，不清空用户当前真实数据和登录凭据。
- [x] 完成四游戏完整数据复核：清单分类、活动标签、挑战去重、地图百分比、时间窗口、未来活动保护、固定任务唯一性和同步状态均通过；四款游戏真实个人链路已分别完成验收。
- [x] 重新生成 `0.1.0-rc.3` 便携试用版并完成解包版、单文件版与打包 MCP 冒烟测试。
- [x] 生成 `0.1.0-rc.4`，修复四游戏周期挑战同一期重复项并自动归并历史重复数据。
- [x] 生成 `0.1.0-rc.5`，完成库街区新版令牌/响应兼容、鸣潮周期与地图真实验收、设置页按钮列对齐及 214 项回归。
- [ ] 更新到 `0.1.0-rc.9` 插件后再次清空测试数据库，完成四游戏首次全局同步的完整覆盖与逐版块个人进度验收；米游社/库街区凭据继续保留。
- [x] 完成最小窗口、Escape 关闭弹窗、键盘焦点、菜单语义、四游戏长标题和持续同步进度的本轮验收；更长时间试用继续由候选版承担。
- [ ] 发布前再次评估 Electron/Node 实验性 `node:sqlite` 的稳定性与迁移方案。
