# 产品与开发待办

更新时间：2026-07-24（Asia/Shanghai）

本文件是后续开发的待办基准；已经确认的新规则优先于旧解析器和旧测试中的完成阈值。

## P0：完成状态与同步正确性

- [x] 四款游戏统一深渊/挑战完成规则：个人接口只要返回“本期存在有效挑战记录”就标记已完成；只有本期完全没有挑战记录才是未完成。不再要求满星、最高难度、全部奖励或最终层，因为每个用户对“打完”的预期不同。
- [x] 为现有官方接口定义结果唯一的“存在记录”机械条件并补测试：原神三种挑战、星铁四种挑战、绝区零两种挑战均执行同一产品语义；不按标题或推测字段判断。鸣潮接入时沿用相同规则并补接口实测。
- [x] 公开资料只能建立挑战名称与周期，绝不能把“有排期”当成用户有挑战记录。
- [x] 周期事项和活动不显示百分比；只有地图探索显示百分比。
- [ ] 重新打包并真实复测原神周期。此前已确认接口成功返回，但旧规则把剧诗 12/12 奖章和幽境难度 5 错判为未完成；最新产品规则将直接以是否存在战绩为准。
- [ ] 实测星铁与绝区零的全部周期模式，检查“碰过即完成、完全没碰才未完成”没有假阴性或假阳性；鸣潮适配器开发时直接使用新规则。

## P0：任务版块版本周期

- [x] 主线任务、支线任务卡片增加当前游戏版本结束倒计时，但界面沿用其他版块的简洁文案，只显示“剩余 ×天/小时/分钟”，不得显示“版本结束倒计时”等额外前缀。
- [x] 为每款游戏保存 `versionKey`、版本开始/结束绝对时间和服务器时区；时间由 Codex 通过中文官方版本公告核验后，可在全局“同步清单”或任务版块“版更校时”时写入。
- [x] 主线与支线共享当前版本结束时间。版本结束后两项自动恢复为未完成并清除旧完成时间与手动完成锁。
- [x] 任务版块增加独立“版更校时”按钮，只校正当前版本键和开始/结束时间，用于官方临时调整版本排期；不得新增、删除主线/支线，不得直接改写两项完成状态。
- [x] 任务版块继续保持固定两项，不提供“删除已完成”和“新增”按钮。
- [x] 为任务版块保存独立同步状态与最后成功时间；只有版本资料成功核验并写入后才更新，失败时保留上一次成功的版本时间。
- [x] 到期但尚未取得下一版本时间时先完成本地重置，并显示“待同步新版本时间”，不得按固定天数猜测结束时间。

## P1：活动玩法标签

- [x] 活动卡片增加一个或多个轻量玩法标签，不新增版块。候选标签可包含签到、战斗、战棋、射击/FPS、跑酷、解谜、音游、经营、肉鸽、剧情等，并允许以后扩展。
- [x] Codex 根据中文官方活动说明、玩法描述和可验证来源给出标签；本地代码只做数量、长度和结构校验，不按活动标题关键词猜测。置信度与证据沿用整条公开资料记录的现有协议。
- [x] 资料不足时由 Codex 写入“待识别”；用户与 AI 都可以修改标签。
- [x] 活动版块增加轻量标签筛选；同一活动允许多个标签。
- [x] SQLite v15 迁移兼容现有活动，旧活动没有标签时不影响显示、排序和同步；个人进度同步不覆盖公开资料已有标签。

## P1：同步状态与界面

- [x] 右上角状态文案明确为“全局清单 · 已同步/未同步”，避免被理解为最近任意一次个人进度同步。
- [x] 保持现有规则：右上角只记录成功的全局 `target=all` 清单同步；活动、周期、地图各自显示版块最后成功时间，版块同步不冒充全局成功。
- [x] 已移除顶部旧的“最近任意同步成功时间”，避免与全局时间同时显示两个日期；当前原神全局最后成功为 2026-07-21 23:08（北京时间）。
- [ ] 完成全局/版块状态标识、时间戳、长标题和窄窗口的视觉验收。
- [x] 全部版块统一倒计时预警：距离结束严格小于 72 小时时，将“剩余 ××”标为红色；等于或大于 72 小时保持普通样式。“距离开始 ××”不使用过期红色预警。
- [ ] 下一次普通同步覆盖测试期间遗留的脱敏诊断成功文案；正式源码已经移除诊断输出。

## P1：三游戏回归与地图

- [ ] 继续原神地图个人进度验收，重点确认沉玉谷父子区域、公开父项与个人子项合并后保持真实 100%。
- [ ] 联网全量复核四款游戏当前已开放地图区域与独立地图层；公开资料同步只增量新增，不删除旧区域、不覆盖个人进度。
- [ ] 星铁实测活动语义候选入队、Codex 领取、证据充分时回写、证据不足时拒绝；防止“反贪「砖」家”一类活动再次误完成。
- [ ] 跨游戏回归活动/周期分类，确保深渊挑战不进入活动版块、未开始活动不能完成、无时间的非活动条目不能混入活动。
- [ ] 原神、星铁遇到 Geetest/1034 时继续一次一版块测试；若官方接口仍不稳定，评估隔离 Electron 官方战绩页会话方案，不读取 Chrome Cookie、不自动破解验证码。

## P2：Codex 与同步体验

- [x] 语义核验队列增加桌面端待处理数量、处理中数量、最近结论和“一键打开 Codex 处理”入口；当前游戏无候选和历史结论时不占界面空间。
- [ ] 把非确定性字段的 Codex 核验逐步扩展到原神、绝区零和鸣潮；软件自身只做可证明唯一的机械转换。
- [ ] Codex 接单后的检索、核验、写入与真实重试次数继续实时显示。接单前传输层重试目前不可观测，不伪造 `1/5`；若以后必须显示，需由应用托管 Codex CLI 事件流。
- [ ] 评估保留联网检索型 AI API 扩展位；只有能真实联网并返回逐项来源的提供方才可接入，普通聊天 API 不用于同步。

## P2：鸣潮、初始验收与发布

- [ ] 完成库街区登录、人工验证窗口、凭据安全存储和鸣潮个人数据能力探测，再按真实能力显示版块“同步进度”。
- [ ] 现有数据回归通过后先创建安全备份，再经用户操作时确认清空测试数据和登录状态，完成一次从零初始全局同步及三游戏完整验收。
- [ ] 完成鸣潮后做四游戏完整验收，覆盖清单、个人记录、时区、倒计时、失败保留、手动完成锁与新用户空状态。
- [ ] 重新生成便携版；之后完成安装版、正式签名、自动更新、发布流程、第三方许可证复核。
- [ ] 补齐小窗口响应式、键盘操作、可访问性和长时间运行测试。
- [ ] 发布前再次评估 Electron/Node 实验性 `node:sqlite` 的稳定性与迁移方案。
