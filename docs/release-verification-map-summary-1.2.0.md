# Gtask 1.2.0 地图修正重新发布验证

日期：2026-09-15，Asia/Shanghai。用户明确要求完成地图方案、测试后仍提交为 1.2.0，覆盖本机并推送远端；此授权包括替换已存在的 `v1.2.0` 标签及附件，不递增至 1.2.1。

## 改动与验证

- 原神、星铁、绝区零和鸣潮共用地图分组逻辑。一级只显示已完成/已收录且已开放子区的 `x/x`，官方一级百分比仅作悬停参考；未知进度不算完成。
- 筛选未完成时保持父子层级，子区全部完成才隐藏整组；新增已开放子区重新显示分组并保留原进度。一级分组不再提供整组手动完成按钮，后端完成接口也拒绝此类操作。
- 无子区一级地区展开同名二级行，显示并操作原有进度；父级统一 `0/1` 或 `1/1`，数据库保持一条记录。真实子区发布后替换同名展示行，不继承旧父级完成状态。
- 公共基准继续决定目录，个人接口只更新匹配项进度；不改公共清单、数据库 schema、版本号或账号数据。
- 完整测试：462 项通过、12 项跳过；类型检查、生产构建、`catalog:check` 和单独启用的性能基准通过。
- 隔离 Windows 打包 UI 验收覆盖四游戏、官方 52% 与子区全部完成、官方 100% 与子区部分完成、未知进度、手动叶节点操作、重启、新子区、540/750 像素窗口；无页面错误。
- 同名二级行专项验证：接口进度、后续快照漏项、手动完成与取消、整组筛选、重启、真实子区加入；额外 240 个独立地图验证虚拟滚动、父子行标识不重复和原记录完成操作。
- UI 证据目录：`C:\Users\Administrator\AppData\Local\Temp\gtask-ui-audit-cvjweg`（不提交测试产物）。

## 本机部署

- 21:04 覆盖 `D:\Git\Gtask` 后启动正常，35 个程序文件逐一匹配验收包。安装版本仍为 1.2.0。
- app.asar SHA-256：`41f479a4cea76524050441e15fc499e4975c6526b459578eed534bd97c0753df`。
- 程序备份、SQLite 在线备份、设置及加密凭据：`D:\Git\Gtask-backups\20260915-210454-pre-map-summary-1.2.0`。
- 安装后数据库完整性为 `ok`，schema 8；加密凭据与部署前一致，自定义/手动事项部署前后均为 0。详情保留在备份目录的 `pre-deployment.json` 和 `deployment.json`。

## 远端发布

发布工作流和实际下载附件验证结果在发布完成后补记。原始 tag 对象为 `3e00bd96e0b45c06766bc555b2f203e797bd176a`、旧源码为 `54249d72277457a5bf64f47e688efc3d6d136f25`，用于限定本次授权替换，避免覆盖并发更新。

在线更新元数据保持 1.2.0，两个下载入口继续指向 GitHub Release。相同版本不会触发已有 1.2.0 客户端的版本变化提示，发布说明明确要求重新下载；本机已直接覆盖。
