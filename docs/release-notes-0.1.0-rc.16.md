# Gtask 0.1.0-rc.16

- 修复 SQLite 多进程写入竞争偶发触发的主进程 `database is locked` 弹窗。
- 个人进度语义核验改用固定、可复用的 Worker 组，不再按候选条数申请 Agent。
- 自动过滤已结束的历史活动和周期候选；地图长期探索数据不受影响。
- 活动玩法标签统一使用中文显示，并在启动时修复历史英文标签。
- 通过 252 项自动化测试及便携包完整性校验。
