# Gtask 0.1.0-rc.19

- Codex 后台处理由固定 4 并发改为动态并发窗口。
- 默认从 2 个 Worker 起步；队列积压且连续稳定完成任务时逐级扩展，当前安全上限为 6。
- 连接重试、传输协议降级或 SQLite 繁忙时快速收缩并进入冷却，不中断正在执行的任务。
- 根据系统可用内存临时限制并发，避免低资源设备因盲目扩容变慢。
- 公开资料与个人语义审核同时积压时为语义审核预留槽位，避免个人进度长期饥饿。
- 超出动态窗口的任务持续排队，空闲槽位出现后自动领取。
