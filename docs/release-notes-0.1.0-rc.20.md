# Gtask 0.1.0-rc.20

- 修复个人地图候选上次被拒绝后，相同数据无法重新进入 Codex 审核、界面直接显示完成的问题。
- 同步清单与同步进度均支持中途取消；个人接口请求、语义审核和对应 Codex Worker 会精确停止，不影响其他并行任务。
- 取消后界面立即退出活动状态并显示“已取消”；语义拒绝不再误报同步成功。
- 便携版 Codex MCP 改用稳定的便携启动入口和持久化运行文件，避免临时解包目录清理后弹出“找不到 exe”。
