# Gtask 基准表维护交接说明

交接时间：2026-08-13（Asia/Shanghai）

产品版本：1.1.0

数据库 schema：v4

当前 MCP 协议：`2026-08-22.1`

## 交接结论

Gtask 已完成从“依赖 Codex 才能使用”到“发布包自带完整持久基准表、个人接口只同步进度”的主流程改造。普通用户启动应用即可看到四款游戏的版本、活动、周期和地图目录；后台 MCP 仅用于维护基准表，不是产品运行前置条件。

本轮 1.1.0 已原位覆盖到 `D:\Git\Gtask`。真实数据库已从旧目录安全迁移到 `文档\Gtask\data\gtask.sqlite` 并升级到 schema v4；迁移前一致性备份、迁移后 `quick_check` 与关键表计数均已核验，凭据未被安装流程重置。本机 Codex 管理插件已切换为 `gtask@personal`。

## 必须保持的架构边界

- 内置版本、活动、周期、地图目录由公共持久基准表所有。
- 米游社/库街区完整个人 snapshot 只更新完成状态、探索度和官方来源绑定，不拥有标题、时间、标签或地图层级。
- 不得重新引入公共数据与个人数据的自动融合、个人语义复核或个人元数据补标链路。
- MCP 是本机 Codex 管理员的专用公共基准维护接口；普通用户没有 MCP 入口，只消费已发布并通过客户端校验的 `updates/catalog.json`。
- 自定义版块保持用户 CRUD；内置卡片不提供新增、编辑和删除。
- 地图严格只有 `region` 与 `subregion` 两级，不得增加第三层或“独立地图”节点。
- 已发布的地图 `remoteKey` 必须稳定。改名应保留旧机器身份，不能按新标题重新生成键。

## 基准表代码入口

| 范围 | 权威文件 | 维护重点 |
| --- | --- | --- |
| 版本窗口、活动 | `src/main/sync/baseline-catalog.ts` | 当前版本起止时间；活动名称、起止时间、玩法标签和来源 |
| 周期模式 | `src/main/sync/cycle-catalog.ts` | 稳定 `modeKey`、当前期预测锚点、周期和持续时间 |
| 地图目录 | `src/main/sync/map-catalog.ts` | 两级目录、父级绑定、稳定键与逐游戏核验时间 |
| 公共维护契约 | `src/main/sync/interface-contract.ts` | MCP 必填字段和 Codex 研究边界 |
| 机械校验和持久化 | `src/main/database.ts` | 标签、时间、身份、换期、个人进度保护和原子写入 |
| MCP 工具入口 | `src/main/local-mcp-server.ts` | 协议登记、任务领取与结构化回写 |

维护地图前必须完整阅读 `docs/ai-map-catalog-maintenance.md`；变更同步架构前必须阅读 `docs/sync-architecture-redesign.md`。

## 强制数据规则

### 版本

- 四款游戏都必须有当前正式版本的 `startsAt` 与 `endsAt`。
- 常规版本按每款游戏的既定周期自动换期；周期没有变化时不要例行发布 `versionWindow`。
- 只有官方延期、缩短、延长或其他既定周期无法表达的例外，才校准版本窗口；优先使用官方明确的下一次维护时刻。
- 不得用卡池、单个活动或奖励领取期限冒充版本结束时间。

### 活动

- 每条活动必须有准确 `startsAt`、`endsAt` 和来源。
- 每条活动必须有 1–5 个有效稳定标签 ID；不得留空、猜测或用“活动”等泛化标签凑数。
- 一个官方活动容器只建一条记录；内部阶段、商店、关卡、奖励档位和卡池不单独建项。
- 修改内置活动后必须通过活动时间、标签质量和稳定身份测试。

### 周期挑战

- 每个内置周期模式必须始终得到当前期明确起止时间，不能再生成无时间占位卡。
- 常规换期完全由稳定模式定义、锚点和周期自动计算；不得按版本推送当期或下期卡片。只有官方调整排期规则/锚点或增删改模式时才维护本节。
- `modeKey` 和 `remoteKey` 表示稳定模式；`periodKey` 表示当期实例。
- 换期前保存上一期历史并重置新一期完成状态；同一期从“等待官方窗口”校准为确定时间时必须原位更新并保留完成状态。
- 个人完成判定只承认本期必须手动挑战部分的记录；自动继承、扫荡、减负结算和快速解锁不算手动完成。

### 地图

- 公共表只维护完整固定目录，个人接口只回写探索度。
- 个人 API 偶尔把基准二级地区作为独立 region 返回；唯一规范化标题可以绑定进度，但渲染层级仍以基准表为准。
- 只更新实际核验过的游戏的 `MAP_CATALOG_VERIFIED_AT`，时间不得写到未来。

## 2026-08-09 基准状态

- `BUNDLED_BASELINE_VERIFIED_AT`：`2026-08-09T12:30:00+08:00`。
- 四款地图目录核验时间：`2026-08-09T12:30:00+08:00`。
- 补齐并修正了本版本的活动时间与玩法标签。
- 五个原先使用 `awaiting-official-window` 的周期模式改为确定性时间锚点。
- 原神地图“虚海垒”更正为“虚海望”，继续使用已发布旧稳定键。
- 绝区零补齐莱姆尼安空洞相关区域和厄匹斯港，共新增 11 个地图条目。

## 2026-08-12 原神 7.0 增量

- 原神版本基准更新为 7.0「无神怜爱的雪国」，官方维护于 2026-08-12 06:00 开始、预计 5 小时，基准上线时间记为 11:00（Asia/Shanghai）。
- 官方尚未公布下一次版本维护时刻；版本终点暂按常规 42 天记为 2026-09-23 05:59，置信度为 0.82。后续官宣若仍符合常规节奏则不更新，只有官方时间偏离常规预测时才通过热更新校准。
- 活动基准更新为“砺行修远”“新芽相助·初探雪原”“铸境研炼·奕思巧战”“险境征者争锋大赛”“至冬漫游手册”；没有把卡池、奖励节点、活动内部阶段或纪行拆成独立事项。
- 周期模式不随版本例行更新当期窗口；深境螺旋、幻想真境剧诗和幽境危战继续由稳定模式定义与内置锚点自动换期。只有官方调整规则/锚点或增删模式时才维护周期基准。
- 地图新增一级区域“至冬”及二级区域“古兽冰原、白桦雪葬地、永凝冻土、焰羽谷、霜殛寒峰”，保持严格两级层级和稳定远程键。
- 内置基准与 `updates/catalog.json` 同步更新；2026-08-13 纠错修订移除例行周期 upsert，归档误建的深境螺旋、幻想真境剧诗下期卡片，并仅保留一次幽境危战稳定行校准来修复旧算法已经产生的错误窗口。
- 周期维护契约 schema 14 明确要求 `modeKey`/`remoteKey` 跨期稳定；普通换期不再作为公开基准增量。带空档的周期严格按 cadence 和锚点跳转，不能把上一期持续时间当成下一期开启间隔。
- 同类周期算法检查覆盖四款游戏。幽境危战与鸣潮“终焉矩阵”的持续时间短于 cadence，存在约 8 天空档；其他当前内置模式为固定月度窗口或持续时间等于 cadence。空档期间显示“距离开始”，连续模式仍正常显示“剩余”。
- 验证结果：周期相关专项回归 99 项通过；完整测试 358 项通过、11 项按既有条件跳过；`pnpm typecheck` 和 `pnpm build` 均通过。

## 2026-08-20 鸣潮 3.6 与绝区零 3.1 核查

- 维护语义统一为“全版块核查、仅提交差异”：先枚举目标版块完整范围并与 `matchCandidates`/`currentVersionWindow` 逐项比较，时间变化才校时，内容变化才增删改，无变化则通过 `verifiedUnchangedTargets` 完成核查，不能重复回写未变化记录。
- 鸣潮内置版本基准更新为 3.6：官方维护为 2026-08-20 04:00–11:00，版本可游玩起点记为 11:00；下一次维护尚未官宣，暂按稳定 42 天节奏记为 2026-10-01 04:00，置信度 0.82。
- 鸣潮活动基准替换为 3.6 当前及已公布活动：群声共振模拟域、第二索拉・诡影迷踪、清弦纪流年、若梦仍有回声、潮汐觅闻、烟云赠礼。`潮汐觅闻` 是官方活动标题，“雾隐阁特别勘察”仅为类型说明，不并入标题。
- 绝区零 3.1 的可游玩起点由维护开始的 06:00 校正为预计维护完成的 11:00，结束边界仍为 2026-09-09 06:00。全活动清单核查后没有新增或删除，只把网页活动标题标点校正为“回归丽都：羽落重逢”。
- 两款游戏的周期与地图版块也完成核查且没有基准差异：鸣潮新增的“全息战略・演武”挑战内容不是新的稳定周期模式；官方所称“玄元境”是随剧情进入的全新场景，现有公开资料未证明它是库街区探索度接口中的独立二级目录，不能据此猜测父级或新增地图进度节点。绝区零 3.1 没有新增稳定周期模式或区域探索目录。
- `updates/catalog.json` 只下发鸣潮 6 个新增活动和绝区零 1 个标题修正；两款游戏的常规 42 天版本节奏没有变化，因此不在热更新中重复发布例行 `versionWindow`。
- 验证结果：相关专项回归 118 项通过；完整测试 364 项通过、11 项按既有条件跳过；`pnpm typecheck`、`pnpm build`、`pnpm package:dir` 均通过。正式目录已覆盖为产品版本 `1.0.0.0`，未改产品版本号。

## 本轮已修复的高风险问题

### 2026-08-22 Gtask 命名迁移与 1.1.0

- 源码目录、窗口 API、AppID、后台 Agent、MCP 工具、资源 URI、插件与 skill 统一为 Gtask 命名；历史名称仅保留在一次性兼容迁移常量和测试中。
- 本地数据目录迁移为 `文档\Gtask`，数据库与备份文件统一使用 `gtask` 前缀；迁移支持 SQLite WAL/SHM、根目录锁回退、冲突拒绝和失败回滚。
- 数据库从 schema v3 原位升级到 v4。真实库 `quick_check=ok`，事项仍为 289 条，其中完成 246、未完成 43；迁移前备份保存在 `文档\Gtask-upgrade-backup-20260822`。
- 公共基准中尚未开始的事项默认隐藏，到达开始时间后自动出现；设置提供“显示尚未开始的基准事项”开关，自定义事项和个人事项不受影响。
- 正式安装升级保留既有 NSIS GUID，Windows 卸载项只有一份并显示 1.1.0；本机 Codex 插件已验证 15 个 MCP 工具和四款游戏后切换到 `gtask@personal`。
- 完整测试 373 项通过、11 项按既有条件跳过；类型检查、构建、目录包、安装器、便携版与 MCP 冒烟均通过。

- 原神地图进度重启后缺失：个人 API 层级与基准层级不同导致漏绑定；现按游戏内唯一标题绑定并保留基准父级。
- “只看未完成”仍显示绿色已完成地图父项：筛选时不再回插已完成祖先，未完成子地区临时提升显示；100% 探索度视为完成。
- 同步过程过于打扰：删除同步过程卡、结果通知、取消按钮及对应死代码；登录要求仍按既有弹窗流程处理。
- 启动重复同步：本地隐藏保存自动同步时间戳，10 分钟内重复启动跳过；手动同步不受限制。
- 启动时报 `UNIQUE constraint failed ... remote_key`：旧周期占位升级确定窗口时错误插入重复稳定键。现在过期周期先换期，同一稳定键时间校准原位更新。

## 验证记录

- `pnpm test`：61 个测试文件通过、1 个文件按条件跳过；373 项通过，11 项按既有条件跳过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- 打包 MCP 冒烟测试：15 个工具、4 款游戏均可读取。
- 使用真实数据库完整副本执行启动迁移：成功；幽境危战补齐时间且保留完成状态，没有重复键。
- 正式程序：`D:\Git\Gtask\Gtask.exe`，产品版本 `1.1.0.0`。
- 当前正式 EXE SHA-256：`30859E8C4EEAF620BED0BB9B2AB92AB6E6306BFF4A4231B8DBD6C6FE30347C14`。
- 1.1.0 安装器 SHA-256：`A914D9CA1BC7A50858E383CF14FB8528E2839B2A378693F96FFFA3DF2B5A52A0`；便携版 SHA-256：`2215D6329BD33953627DB3A33818FB9C1B1A81B4C508A33299B5241C759D28C1`。
- 当前线上清单 revision：`2026-08-13.genshin-cycle-identity-correction`；本机于 2026-08-13 23:21（Asia/Shanghai）通过 Gitee 成功手动应用。

## 受保护的本机数据

- 正式数据库：`D:\Users\Administrator\Documents\Gtask\data\gtask.sqlite`
- 凭据目录：`C:\Users\Administrator\AppData\Roaming\gtask\credentials`
- 正式程序目录：`D:\Git\Gtask`

不得删除、重建、重置或打包上述数据库和凭据。测试必须使用临时数据库或正式数据库的完整副本。保留备份、发布参考和用户未跟踪研究目录。

## 下一窗口建议流程

1. 阅读根目录 `AGENTS.md`、本文件、`docs/next-session.md` 和 `docs/sync-architecture-redesign.md`。
2. 若维护地图，再完整阅读 `docs/ai-map-catalog-maintenance.md`。
3. 检查 `git status` 和最新提交，确认工作树干净。
4. 基准表维护契约版本为 `2026-08-22.1`。插件上报版本仅作诊断，不得阻塞本机 Codex 管理端；始终以 MCP 工具 schema 和领取到的 `job.contract` 为准，也不要调用已退役的个人复核链路。
5. 联网优先使用官方公告、官方活动说明和官方地图资料；社区资料只用于交叉核验。
6. 更新相应基准、来源和实际受影响游戏的核验时间，同时保持稳定机器身份。
7. 先跑针对性测试，再依次执行 `pnpm typecheck`、`pnpm test`、`pnpm build`。
8. 只有用户明确要求部署时才覆盖正式目录；覆盖时保留外部数据库、凭据和备份。

## 待用户验收

- 修复版尚待用户实际打开 GUI 验收。
- 重点确认四款游戏正常启动、周期卡片都有时间、原神地图进度重启保持、未完成筛选正确、10 分钟自动同步冷却生效。
- 当前没有已知代码阻塞项；下一阶段主要工作是按版本维护公共基准表。
