# 提前显示修复与原神清单发布记录

日期：2026-09-24，Asia/Shanghai。用户授权修复、检查所有游戏和版块、提交代码与清单并推送；随后授权更新本机并明确禁止创建更新备份。软件版本保持 1.2.0，未发布 Release。

## 根因与覆盖

- `filterUpcomingBaselineItems` 对 `endgame` 无条件放行，旧测试和设置说明也将此例外当作预期。因此原神幽境危战在下一期尚未开放时仍显示；四个游戏使用相同路径，均受影响。
- 移除周期例外，公共限时活动和周期统一按真实开始时间及开关过滤。手动/自定义事项不受影响，缺少或无效时间不会被伪装成未来事项。现有时钟包含所有条目的开始时间，到边界自动显示。
- 地图目录只收录当下已开放地图，不提供未来地图预览。地图树直接使用完整目录并保留原有开放资格检查，不经过活动/周期的预览开关；地图身份、层级、完成计数规则和数据库结构不变。

## 验证

- 修复前新增测试在四游戏及开启边界共 5 个场景失败，均显示未来周期被错误保留；修复后通过。
- 专项测试：提前显示、地图树、地图目录/新鲜度及公共清单消费共 53 项通过。
- `pnpm test`：74 个测试文件通过、2 个条件文件跳过；467 项通过、12 项条件测试跳过。
- `pnpm build`：Node/renderer 类型检查和生产构建通过。
- `pnpm catalog:check`：实际消费器在隔离内存数据库中成功应用清单；新 revision 为 `2026-09-24.genshin-7.1-baseline-maintenance`，协议仍为 v1。
- `scripts/audit-ui-smoke.mjs`：隔离 Electron 验收通过。覆盖四游戏逐个执行关闭→开启→关闭、已开启事项与未来自定义事项保持可见、重启保持关闭、事项数据切换前后一致；同时通过原有增删改、回收站、备份、地图折叠和缺截止时间展示检查，无页面异常。
- UI 验收仅在脚本创建的临时目录中使用合成活动和周期样本，没有读写正式玩家数据库或凭据。

## 清单与兼容性

前一轮本机维护任务 `a752faf3-460f-49d2-8b12-4ebad1dc19bb` 已核验并加入 6 项活动，本轮复用其证据和稳定键；公开结构导出排除了个人状态。完整数据、来源和未解决事项见 [研究记录](catalog-research/genshin-7.1-2026-09-24.json)。

通过差异编译器发布到 `updates/catalog.json` 的是银翎溯月，玄鸟结璘、铸境研炼·巧策进斗、耀星烁熠·累计登录，共 3 项。保留原累计清单及退役记录，不更新常规周期或版本锚点，不修改其他游戏事实。新增消费回归证明与本机已有同稳定键活动合并时不重复、不重置完成和手动事项。

巡历雪境，凝铸锋锐、桌上剧团·冒险的前夜会使用 `custom.character-invitation`，判测胜券本征使用 `custom.outcome-prediction`。这两个标签只在本机维护任务注册；当前公共 v1/v2 协议均不能携带定义，未注册标签的 1.2.0 客户端会拒收包含它们的整批清单。因此完整数据随研究记录提交，暂不放入在线公共清单，不以不准确的签到/问答等标签替代。正式解决需要兼容的标签分发协议及客户端能力，本轮显示修复不扩大为该协议变更。

绮星盛会、绮衣珍赏·晴暖的公告只给出版本更新后开始，未获得独立精确开始边界，继续保留为研究候选。地图及周期/版本规则沿用已核验基准。公共清单热更新只传递活动事实；其他客户端需更新程序才会获得显示修复。

## 后续本机更新

22:40 按用户的新授权，将 `9d2475a` 构建的程序直接覆盖到 `D:\Git\Gtask` 并成功启动，版本仍为 1.2.0。未复制旧程序、数据库、设置或凭据，更新前后备份目录名称清单一致。35 个已安装程序文件均与构建包 SHA-256 一致，包内构建文件及随附运行时也已逐一校验。

安装后数据库完整性为 `ok`、schema 8；自定义/手动保护状态、加密凭据和设置保留。app.asar SHA-256 为 `7765d27ead5f0675bc8c72e00c5a5dd3d62805912b48149d6d5171451aed44c6`。本机更新默认不创建备份的长期项目约定已写入 `AGENTS.md` 和 `next-session.md`，仅用户明确要求时才备份。
